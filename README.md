# Electron Tray Weather App (TypeScript)

Простое приложение на **Electron + TypeScript**, которое показывает текущую температуру и погодные условия в **иконках в системном трее** и обновляет их **каждую минуту** (или с настраиваемым интервалом).

Используется бесплатное API `open-meteo.com` (без ключа).

## Возможности

- 🌡️ **Две иконки в трее**:
  - Первая иконка показывает текущую температуру в виде текста (например, `5°`, `-13°`)
  - Вторая иконка показывает погодные условия (эмодзи: ☀️, ☁️, 🌧️ и т.д.)
- 🔄 **Автоматическое обновление** — данные обновляются каждую минуту (или с настраиваемым интервалом)
- 📍 **Гибкая настройка местоположения** — можно указать либо координаты, либо город и страну (взаимно исключающие варианты)
- 🕐 **Время последнего обновления** — показывается в tooltip и меню с точностью до секунды, включая часовой пояс и UTC offset
- 📊 **Информация о местоположении** — координаты, город и страна в контекстном меню
- 📋 **Подробная информация о погоде** — окно с детальными параметрами (ощущается как, ветер, облачность, давление, влажность, видимость, точка росы, комфорт, осадки, восход/закат, УФ индекс и др.)
- 🐛 **История ошибок API** — сохраняются последние 20 ошибок для отладки
- 📡 **История API-запросов** — сохраняются последние 20 запросов с полными деталями (URL, заголовки, тело ответа)
- ⚙️ **Удобные настройки** — диалог с быстрым выбором популярных городов
- 🔔 **Системные уведомления** — уведомления о сохранении настроек
- 🖥️ **Умное позиционирование окон** — все окна открываются на основном мониторе
- 🔒 **Предотвращение дублирования окон** — повторное открытие активирует уже открытое окно

## Как запустить

Открыть раздел "Releases" в гитхаб-репозитории. Открыть последний релиз. Скачать архив, соответствующий используемой операционной системе, разархивировать, запустить.

### Linux

Скачать `electron-tray-weather-1.0.0.zip`, разархивировать, запустить (`ELECTRON_DISABLE_SANDBOX=1 ./electron-tray-weather`)

### Windows

Скачать `Tray.Weather-1.0.0-win.zip`, разархивировать, запустить exe-файл ("Tray Weather.exe")

Windows может ругнуться на запуск программы из непроверенного источника

В Windows может быть необходимо включить скрытую иконку

### macOS

Использовать архив `Tray.Weather-1.0.0-mac.zip`

## Как работает приложение

После запуска приложения **не открывается окно** — приложение работает в фоновом режиме. В системном трее (область уведомлений) появляются **две иконки**: одна с температурой, другая с погодными условиями.

**Взаимодействие с приложением:**
- **Левый клик** по любой иконке — открывает окно с подробной информацией о погоде
- **Правый клик** по любой иконке — открывает контекстное меню с различными опциями:
  - Просмотр текущей температуры и информации о местоположении
  - Обновление температуры вручную
  - Открытие окна "Подробная информация о погоде"
  - Открытие диалога "Настройки"
  - Просмотр справки "Как пользоваться"
  - Просмотр истории ошибок API
  - Просмотр истории API-запросов
  - Выход из приложения

## Установка

```bash
npm install
```

При первом запуске автоматически создастся файл `settings.json` с дефолтными настройками.

## Запуск через npm

### Режим разработки

```bash
npm run dev
```

### Сборка и запуск

```bash
npm run build
npm start
```

## Настройка местоположения

Настройки хранятся в файле `settings.json` в корне проекта. При первом запуске файл создаётся автоматически с дефолтными значениями.

> **⚠️ Важно:** По умолчанию температура показывается для **New York City, United States**. Чтобы показывалась температура для вашей актуальной локации, необходимо изменить настройки через диалог "Настройки" или отредактировать файл `settings.json` вручную.

> **⚠️ Важно:** Необходимо указать **либо** город и страну, **либо** координаты. Оба варианта одновременно указывать нельзя.

### ⚙️ Диалог "Настройки" (рекомендуется)

Вместо ручного редактирования файла `settings.json` можно использовать удобный диалог настроек:

1. Кликните правой кнопкой мыши на иконку в трее
2. Выберите пункт **"Настройки"**
3. В открывшемся окне заполните необходимые поля:
   - **Город и Страна** — координаты будут определены автоматически
   - **Широта и Долгота** — название города и страны будут определены автоматически
4. Используйте **быстрый выбор города** для популярных городов (Warsaw, Minsk, Astana, Berlin, Paris, New York)
5. Нажмите кнопку **"Сохранить"**

Диалог автоматически проверит корректность введённых данных и применит настройки. Приложение продолжит работать с новыми настройками без перезапуска. После сохранения появится системное уведомление.

### 📝 Ручное редактирование файла settings.json

Альтернативный способ — отредактировать файл `settings.json` вручную. Необходимо указать один из вариантов:

#### Вариант 1: Указать город и страну

Отредактируйте файл `settings.json`:

```json
{
  "city": "New York City",
  "country": "United States",
  "latitude": null,
  "longitude": null,
  "updateIntervalInSeconds": 60
}
```

Координаты будут определены автоматически через Geocoding API.

#### Вариант 2: Указать координаты напрямую

Отредактируйте файл `settings.json`:

```json
{
  "city": null,
  "country": null,
  "latitude": 53.9045,
  "longitude": 27.5615,
  "updateIntervalInSeconds": 60
}
```

Название города и страны будут определены автоматически по координатам через Nominatim (OpenStreetMap) API.

### Настройка интервала обновления

Поле `updateIntervalInSeconds` определяет, как часто обновляется температура (в секундах). Значение по умолчанию: `60` (1 минута).

Пример для обновления каждые 30 секунд:

```json
{
  "updateIntervalInSeconds": 30
}
```

## Отображение информации

### Иконки в трее

- **Первая иконка** — температура отображается **прямо на иконке** в виде текста (например, `5°`, `-13°`)
- **Вторая иконка** — погодные условия отображаются в виде эмодзи (☀️, ☁️, 🌧️, ❄️ и т.д.)
- Иконки обновляются каждую минуту (или с настраиваемым интервалом)

### Tooltip (при наведении)

Показывает одинаковую информацию для обеих иконок:
- Название города и страны (если доступно)
- Текущую температуру
- Ощущается как (feels like)
- Время последнего успешного обновления с секундами, часовым поясом и UTC offset

Пример: `Нью-Йорк, США\nТемпература: 0.2 °C\nОщущается как: -2.5 °C\nОбновлено: 2024-01-15T14:35:42 (America/New_York, UTC-05:00)`

### Контекстное меню (правый клик)

Содержит:
- Текущую температуру
- Время последнего обновления
- Координаты (LATITUDE и LONGITUDE)
- Город и страну (если доступны)
- Пункт **"Обновить сейчас"** для ручного обновления
- Пункт **"Подробная информация о погоде"** для открытия окна с детальными параметрами
- Пункт **"Настройки"** для редактирования настроек через диалог
- Пункт **"Как пользоваться"** для просмотра справки
- Пункт **"Показать ошибки API (N)"** для просмотра истории ошибок
- Пункт **"Показать последние API-запросы"** для просмотра истории запросов
- Пункт **"Выйти"**

### Подробная информация о погоде

Окно с детальными параметрами погоды открывается:
- **Левым кликом** по любой иконке в трее
- Через пункт меню **"Подробная информация о погоде"**

Окно показывает:
- Текущую температуру
- Ощущается как
- Ветер (скорость в м/с, направление, описание)
- Облачность (%)
- Давление (гПа)
- Влажность (%)
- Видимость (км)
- Точку росы (°C)
- Уровень комфорта
- Осадки (мм)
- Восход солнца (с часовым поясом и UTC offset)
- Закат солнца (с часовым поясом и UTC offset)
- Продолжительность светового дня
- УФ индекс
- Время обновления (с часовым поясом и UTC offset)

Все времена отображаются в том же формате, в котором они были получены от API, с указанием часового пояса и UTC offset.

## История ошибок API

Приложение сохраняет последние 20 ошибок взаимодействия с API. Для просмотра:

1. Кликните правой кнопкой мыши на иконку в трее
2. Выберите пункт **"Показать ошибки API (N)"**
3. Откроется окно с подробной информацией о каждой ошибке:
   - Время возникновения
   - Название API
   - Текст ошибки
   - URL запроса (если доступен, кликабельный)
   - HTTP статус код (если доступен)
   - Код ошибки Node.js (если доступен)

Если ошибок нет, откроется окно с сообщением об отсутствии ошибок.

## История API-запросов

Приложение сохраняет последние 20 API-запросов с полными деталями. Для просмотра:

1. Кликните правой кнопкой мыши на иконку в трее
2. Выберите пункт **"Показать последние API-запросы"**
3. Откроется окно с подробной информацией о каждом запросе:
   - Время запроса
   - Название API
   - URL запроса (кликабельный)
   - HTTP метод
   - Заголовки запроса
   - HTTP статус код ответа
   - Заголовки ответа
   - Полное тело ответа (JSON)
   - Длительность запроса (мс)

Если запросов нет, откроется окно с сообщением об отсутствии запросов.

## Технические детали

- **Язык**: TypeScript
- **Фреймворк**: Electron
- **API погоды**: Open-Meteo (https://api.open-meteo.com)
- **API геокодирования (прямое)**: Open-Meteo Geocoding (https://geocoding-api.open-meteo.com)
- **API геокодирования (обратное)**: Nominatim (OpenStreetMap) (https://nominatim.openstreetmap.org)
- **Создание иконок**: Canvas (для отрисовки текста температуры и эмодзи)
- **Конфигурация**: settings.json файл
- **Кэширование эмодзи**: Только на Linux (для оптимизации производительности)

## Структура проекта

```
weather/
├── src/
│   ├── main.ts          # Основной файл приложения
│   └── canvas.d.ts      # Типы для canvas
├── dist/                # Скомпилированный JavaScript
├── settings.json        # Настройки местоположения (создаётся автоматически)
├── package.json
├── tsconfig.json
└── README.md
```

## Зависимости

- `electron` — фреймворк для десктопных приложений
- `canvas` — библиотека для создания изображений с текстом

## Лицензия

MIT
